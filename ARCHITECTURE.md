# 🏥 المساعد الطبي الذكي (Smart Medical Assistant) — الدليل الشامل لهندسة النظام

هذا الملف يمثل التوثيق البرمجي والهندسي الكامل لمشروع "المساعد الطبي الذكي". تم إعداد هذا الملف لتوضيح حجم الجهد المبذول، التقنيات المستخدمة، وكيفية بناء النظام بأسس برمجية متينة ومطابقة لمعايير تطوير البرمجيات الحديثة. 

---

## 📋 الفهرس
1. [التقنيات والأدوات واللغات المستخدمة](#1-التقنيات-والأدوات-واللغات-المستخدمة)
2. [البنية التحتية ومعمارية النظام (Architecture)](#2-البنية-التحتية-ومعمارية-النظام-architecture)
3. [هيكلية شجرة الملفات (File Tree Structure)](#3-هيكلية-شجرة-الملفات-file-tree-structure)
4. [آليات الأمان والحماية (Security Measures)](#4-آليات-الأمان-والحماية-security-measures)
5. [تحسين الأداء وتجربة المستخدم (Performance & UX)](#5-تحسين-الأداء-وتجربة-المستخدم-performance--ux)
6. [دورة حياة الطلب مع الذكاء الاصطناعي (AI Request Lifecycle)](#6-دورة-حياة-الطلب-مع-الذكاء-الاصطناعي-ai-request-lifecycle)
7. [تفاصيل الجداول وقواعد البيانات (Database Schema)](#7-تفاصيل-الجداول-وقواعد-البيانات-database-schema)
8. [شرح تفصيلي لوظائف الملفات الأساسية](#8-شرح-تفصيلي-لوظائف-الملفات-الأساسية)

---

## 1. التقنيات والأدوات واللغات المستخدمة
تم بناء هذا النظام باستخدام حزمة تقنيات (Tech Stack) حديثة ومتقدمة جداً، مقسمة كالتالي:

### 💻 لغات البرمجة:
- **PHP 8.2**: اللغة الأساسية للواجهة الخلفية (Backend)، وتتميز بالسرعة العالية ودعم البرمجة الكائنية (OOP).
- **JavaScript (ES6+)**: لغة الواجهة الأمامية الأساسية للتعامل مع التفاعلات الحية.
- **HTML5 & CSS3**: لبناء الهيكل وتنسيق الواجهات.

### ⚙️ أطر العمل (Frameworks):
- **Laravel 11**: أقوى إطار عمل في عالم PHP، يُستخدم لبناء خوادم الـ API، إدارة قواعد البيانات، وتوفير بيئة آمنة للمشروع.
- **Vue.js 3 (Composition API)**: إطار عمل الواجهة الأمامية. تم استخدامه لبناء تطبيق صفحة واحدة (SPA - Single Page Application) ليكون التطبيق سريعاً كالبرامج الأصلية ولا يحتاج لإعادة تحميل الصفحة.
- **TailwindCSS v4**: إطار عمل لتصميم الواجهات بأسلوب (Utility-first)، مما يعطي تصميماً عصرياً، متجاوباً مع الهواتف، ويدعم الوضع الليلي (Dark Mode) بسهولة.

### 🧠 الذكاء الاصطناعي (AI Integration):
- **Google Gemini 3.1 Flash API**: تم دمج هذا النموذج اللغوي الضخم كـ "عقل طبي" للنظام. يتميز بسرعته الفائقة في معالجة النصوص وتقديم استشارات بناءً على المعطيات المعطاة.

### 🛠️ الأدوات والمكتبات الإضافية:
- **Laravel Sanctum**: نظام حماية وإصدار رموز مصادقة (Bearer Tokens).
- **Vite**: أداة بناء الجيل القادم (Next-Generation Frontend Tooling) لترجمة ملفات Vue و CSS وجعلها خفيفة وسريعة.
- **Axios**: مكتبة مسؤولة عن إرسال طلبات الـ HTTP في الخلفية (AJAX).
- **Vue Router 4**: موجه مسارات الواجهة الأمامية للانتقال بين الشاشات فورياً.
- **Marked.js**: مكتبة تقوم بتحويل النصوص الطبية القادمة من الذكاء الاصطناعي (Markdown) إلى نصوص منسقة بصرياً (جداول، نقاط، عناوين).

---

## 2. البنية التحتية ومعمارية النظام (Architecture)
تم تصميم النظام ليكون قابلاً للتوسع (Scalable) والصيانة (Maintainable) باستخدام أنماط هندسية معتمدة عالمياً:

1. **نمط API-Driven Development:** الواجهة الخلفية (Laravel) لا تقوم بطباعة واجهات مرئية أبداً؛ بل ترد دائماً ببيانات خام بصيغة `JSON`. هذا يعني أنه يمكن لاحقاً ربط نفس السيرفر بتطبيق موبايل (Android/iOS) دون تغيير سطر كود واحد في الـ Backend.
2. **نمط MVC (Model-View-Controller):** تم استخدامه في طبقة Laravel لتنظيم الكود (المتحكمات تستقبل الطلب، النماذج تتخاطب مع القاعدة).
3. **نمط Clean Architecture (فصل الخدمات):** تم فصل كود التواصل مع الذكاء الاصطناعي في ملف خدمة مستقل `GeminiMedicalService`. المتحكم لا يعرف كيف يتواصل مع جوجل، فقط ينادي هذه الخدمة لتقوم بالعمل، مما يقلل تداخل الأكواد.
4. **تطبيق الصفحة الواحدة (SPA):** بفضل Vue.js، النظام يحمل صفحة HTML واحدة فقط، ثم يقوم باستبدال المكونات (Components) في أجزاء من الثانية، مما يوفر تجربة مستخدم خرافية وخالية من الـ Refresh.

---

## 3. هيكلية شجرة الملفات (File Tree Structure)
هذا الهيكل يوضح الترتيب المنطقي لملفات المشروع، مما يدل على تنظيم عالٍ وفصل واضح للوظائف:

```text
ai-medical-assistant/
│
├── app/                              # العقل المدبر للسيرفر (Backend)
│   ├── Http/
│   │   ├── Controllers/Api/          # المتحكمات (Controllers)
│   │   │   ├── AuthController.php    # (إدارة الدخول والتسجيل)
│   │   │   ├── ChatController.php    # (إدارة الدردشات والمحادثات)
│   │   │   └── ProfileController.php # (إدارة الملف الشخصي الطبي)
│   │   └── Middleware/               # (بوابات التفتيش الأمني)
│   │
│   ├── Models/                       # نماذج قاعدة البيانات (Models)
│   │   ├── User.php
│   │   ├── ChatSession.php
│   │   └── Message.php
│   │
│   └── Services/                     # الخدمات الخارجية المستقلة
│       └── GeminiMedicalService.php  # (التخاطب مع ذكاء جوجل)
│
├── config/                           # إعدادات النظام الداخلية
├── database/                         
│   ├── migrations/                   # ملفات بناء الجداول برمجياً
│   └── database.sqlite               # قاعدة البيانات الفعلية
│
├── resources/                        # واجهة المستخدم (Frontend)
│   ├── css/
│   │   └── app.css                   # إعدادات TailwindCSS
│   └── js/
│       ├── api/
│       │   └── axios.js              # أداة التخاطب مع الـ API
│       ├── components/               # القطع القابلة لإعادة الاستخدام
│       │   ├── ChatWindow.vue        # (نافذة الدردشة)
│       │   ├── MessageBubble.vue     # (فقاعة الرسالة المنفردة)
│       │   ├── Sidebar.vue           # (القائمة الجانبية)
│       │   └── OnboardingModal.vue   # (شاشة إكمال الملف الطبي)
│       ├── composables/              # أدوات مساعدة عامة
│       │   ├── useLang.js            # (نظام الترجمة الفورية والـ RTL)
│       │   └── useTheme.js           # (نظام الوضع الليلي/النهاري)
│       ├── router/
│       │   └── index.js              # (موجه المسارات للانتقال بين الشاشات)
│       ├── stores/
│       │   └── auth.js               # (الذاكرة المؤقتة لبيانات المستخدم)
│       ├── views/                    # الصفحات الرئيسية الكاملة
│       │   ├── LoginView.vue
│       │   ├── RegisterView.vue
│       │   ├── ChatView.vue
│       │   └── ProfileView.vue
│       ├── App.vue                   # الحاوية الرئيسية للتطبيق
│       └── app.js                    # نقطة تشغيل الـ Vue
│
├── routes/
│   └── api.php                       # مسارات الـ API للواجهة الخلفية
│
├── .env                              # ملف المتغيرات السرية (كالمفاتيح)
├── vite.config.js                    # إعدادات المترجم Vite
└── package.json & composer.json      # مكاتب واعتمادات المشروع
```

---

## 4. آليات الأمان والحماية (Security Measures)
لإظهار قوة النظام، تمت إضافة طبقات حماية متطورة جداً:
1. **Bearer Tokens (Sanctum):** لا توجد جلسات تقليدية (Cookies Session). بدلاً من ذلك، النظام يعتمد على الـ Tokens. لا يستطيع أي شخص جلب بيانات محادثة أو كتابة رسالة إلا إذا كان يمتلك Token صحيح وخاص به.
2. **CORS Protection:** السيرفر يقبل الطلبات من مصادر معتمدة فقط، لمنع اختراقات الـ Cross-Origin.
3. **Data Validation:** قبل إدخال أي بيانات لقاعدة البيانات (سواء عند التسجيل أو إضافة عمر/وزن)، تمر البيانات على فلتر في Laravel يتأكد أنها بالأرقام والصيغ الصحيحة.
4. **Environment Variables (`.env`):** لم يتم وضع مفتاح الذكاء الاصطناعي أو كلمات سر القاعدة في الكود المصدري أبداً؛ بل عُزلت في ملف سرّي `.env` لمنع تسريبها.

---

## 5. تحسين الأداء وتجربة المستخدم (Performance & UX)
لم يكن التركيز على "تشغيل" النظام فقط، بل ليكون بلمسة إبداعية تنافس التطبيقات العالمية:
1. **Optimistic UI (الواجهة المتفائلة):** عندما يرسل المريض رسالة، لا تنتظر الواجهة رد السيرفر لرسمها! يتم رسم فقاعة المريض فوراً في الشاشة لتعطي إحساساً بالسرعة الصاروخية، بينما يتم الإرسال للسيرفر في الخلفية.
2. **ترجمة فورية (i18n & RTL):** النظام مصمم ليغير اللغة من العربي للإنجليزي والعكس، مع قلب اتجاه الشاشة من اليمين لليسار، **بضغطة زر وبدون تحميل الصفحة**.
3. **Caching (التخزين المحلي):** يتم حفظ اللغة المفضلة، الوضع الليلي، والـ Token في الـ `localStorage` بحيث يجدها المستخدم جاهزة عند فتح الموقع في اليوم التالي.

---

## 6. دورة حياة الطلب مع الذكاء الاصطناعي (AI Request Lifecycle)
كيف يفكر الذكاء الاصطناعي؟ هذه هي الخطوات البرمجية الخفية:
1. المريض يكتب "عندي صداع شديد وحرارة".
2. يصل الطلب لـ `ChatController`، والذي يستدعي الـ `ProfileController` لقراءة ملف المريض (عمره 45، وزنه 90، يعاني من السكري).
3. يستدعي `ChatController` آخر 20 رسالة بين المريض والذكاء الاصطناعي (لضمان تذكر سياق الكلام).
4. يرسل كل هذه البيانات إلى `GeminiMedicalService`.
5. تقوم الخدمة بكتابة أمر خفي (System Prompt) لجوجل: 
   *"أنت مساعد طبي محترف. هذا المريض عمره 45 ومصاب بالسكري. بناء على ذلك وبناء على تاريخ محادثته، أجب عن سؤاله (عندي صداع وحرارة). حذره من أي شيء يرفع السكر، ولا تصرف أدوية وصفية."*
6. يرد Gemini بالنص، ويتم تحويل هذا النص المعقد من خلال مكتبة `Marked.js` في الواجهة الأمامية ليظهر في الشاشة بشكل فقاعة جميلة.

---

## 7. تفاصيل الجداول وقواعد البيانات (Database Schema)
استخدمنا **SQLite** (قاعدة بيانات ملفية سريعة جداً):
- **جدول `users`:** يحمل بيانات تسجيل الدخول وتفاصيل الجسد (الوزن، فصيلة الدم، العمر، والجنس) والأمراض المزمنة.
- **جدول `chat_sessions`:** لفصل الاستشارات عن بعضها. المريض يمكنه فتح جلسة للصداع، وجلسة أخرى لآلام الظهر. الجلسات تُرتب تصاعدياً، ويمكن وضع "دبوس" `is_pinned` لرفع الجلسة المهمة لأعلى القائمة.
- **جدول `messages`:** يحمل كل رسالة، رقم الجلسة التي تنتمي إليها، ومن الذي أرسلها (المريض أم الطبيب الذكي). يتميز بأنه يخزن تاريخ كل رسالة بالثانية.

---

## 8. شرح تفصيلي لوظائف الملفات الأساسية

- **`routes/api.php`**: مركز الاستقبال. هنا مسارات الدخول (لا تطلب Token)، ومسارات المحادثة وتعديل الملف الطبي (محمية ومغلقة بـ Middleware).
- **`axios.js`**: موظف التوصيل. تم برمجته ليعترض أي طلب خارج للسيرفر ويلصق به الـ Token السري، كما أنه يعترض أي رد فيه الخطأ `401 Unauthorized` ليقوم بطرد المستخدم لشاشة الدخول تلقائياً.
- **`app.css`**: ملف صغير جداً لكنه يحمل محرك `TailwindCSS v4` القوي، والذي يقوم بقراءة كل كلاسات التصميم الموجودة في الكود وبناء ستايل خاص للمشروع فقط، مما يجعل الموقع خفيفاً جداً.
- **`1-Setup.bat` & `2-Run_Server.bat`**: أدوات تم برمجتها بـ (Batch Scripting) لأتمتة وتشغيل السيرفرات بضغطة زر واحدة بدون الحاجة لمعرفة المبرمج بكتابة الأوامر المعقدة.

---

> **خلاصة للمناقشة:** 
> هذا المشروع ليس مجرد موقع دردشة بسيط؛ إنه نظام برمجي متكامل يفصل بين طبقات البيانات والواجهات والذكاء الاصطناعي، ويستخدم أحدث التقنيات لضمان السرعة والأمان وتجربة المستخدم الخالية من العيوب.
